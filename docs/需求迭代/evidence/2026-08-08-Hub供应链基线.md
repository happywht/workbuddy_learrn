# Hub 供应链基线（2026-08-08）

## 结论

Hub 已在本机建立可重复执行的依赖锁、依赖审计、镜像漏洞门禁和 CycloneDX SBOM。最终扫描通过且没有使用漏洞例外：

| 项目 | 结果 |
|---|---|
| `requirements.lock` SHA-256 | `a325dfd2d6e0e38a87354844e878969b2c30ec47f4bf61bd1a0bffb741094d0a` |
| `pip-audit 2.9.0` | 46 个生产依赖，0 个漏洞 |
| Trivy 0.66.0 | 0 个 OS/Python 包漏洞，0 个 HIGH/CRITICAL |
| CycloneDX | 1.6；100 个组件（99 library + 1 operating-system） |
| 漏洞例外 | 0 |
| 本地临时镜像 ID | `sha256:28d3c99f2ed6bbf819727c0400039526fadfa75f8b8683f67cd44d41b6f6f34c`；扫描后已删除 |

机器可读报告位于本地忽略目录 `artifacts/supply-chain/`，CI 会将同类报告作为 30 天构建产物保存。

## 发现与修复过程

初始固定的 `python:3.12-slim`（Debian 13.6）扫描发现 178 项，其中 4 CRITICAL、19 HIGH；全部阻断项来自 OS 基础层，Python 依赖审计为 0，且报告当时没有给出可用修复版本。因此没有创建 23 条风险例外，而是：

1. 切换到固定 digest 的 `python:3.12-alpine`；
2. 用哈希锁和 `--only-binary=:all:` 验证全部 musllinux wheel 可用；
3. 删除最终运行时不需要且带 4 MEDIUM、1 LOW 的基础镜像 `pip 25.0.1`；
4. 再次对最新源码构建执行完整门禁，最终漏洞数为 0。

## 运行态回归

独立 Compose 项目 `workbuddy-hub-supply-chain-ci` 使用端口 `18180/55510` 和独立 volume 完成 PostgreSQL 健康检查、Alembic 启动迁移和 Hub smoke：

- Hub 容器 UID 为 10001；
- `python -m pip` 不存在；
- `/health` 正常，4 个案例可读；
- `case-capacity` 标题仍为 `项目资料交付检查`；
- MCP 返回 18 个工具；
- 请求 ID 和 Prometheus 指标正常。

验证后已删除临时容器、网络、volume 和 Compose 镜像；现有 `127.0.0.1:8100` 未参与测试，之后仍返回 health ok 和 4 个案例。

## 证据边界

结果来自 2026-08-08 的本机 Docker Linux amd64 构建和当时的漏洞数据库快照。它不等同 GitHub Actions 已运行，也不证明镜像已推送、签名、生成来源证明、覆盖其他架构或通过生产 Kubernetes 准入。漏洞数据库变化后结果可能变化，必须在每次发布重新执行。
