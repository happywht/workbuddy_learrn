global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/hub-alerts.yml

scrape_configs:
  - job_name: workbuddy-hub-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - hub-api:8000
        labels:
          environment: poc
